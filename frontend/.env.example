# 订阅 JWT 由浏览器随请求发给**本地/自托管业务 API**；校验在业务进程内请求订阅服务完成。
# 订阅服务根地址（勿尾斜杠）请在运行 standalone API 的进程上设置 SUBSCRIPTION_API_URL（见 standalone 启动说明）。

# 仅前端构建：已不再用于关闭校验；关闭订阅检查请设业务 API 环境变量 SUBSCRIPTION_OFF=1（VS Code 扩展宿主可用 DBPLAYER_SUBSCRIPTION_OFF=1）。

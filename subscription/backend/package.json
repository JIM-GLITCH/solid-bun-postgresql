{
  "name": "subscription-api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "dev:all": "bun run --watch dev-with-portal.ts",
    "start": "bun run src/index.ts",
    "build": "bun run build.ts",
    "deploy": "bun run deploy.ts"
  }
}
